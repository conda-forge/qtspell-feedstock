{% set name = "qtspell" %}
{% set version = "1.0" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/manisandro/qtspell/archive/refs/tags/{{ version }}.tar.gz
  sha256: a803c334f87be991397805716f9fd9caaa8ce1da25e808eb9715be38dbca812a

build:
  number: 0
  skip: true  # [not linux]
  run_exports:
    - {{ pin_subpackage('qtspell', max_pin='x.x') }}

requirements:
  build:
    - cmake
    - ninja
    - pkg-config
    - {{ compiler('cxx') }}
    - {{ cdt('mesa-libgl-devel') }}  # [linux]
    - {{ cdt('mesa-dri-drivers') }}  # [linux]
    - {{ cdt('libselinux') }}  # [linux]
    - {{ cdt('libxdamage') }}  # [linux]
    - {{ cdt('libxxf86vm') }}  # [linux]
    - {{ cdt('libxext') }}     # [linux]
  host:
    - xorg-libxfixes  # [linux]
    - qt
    - enchant
  run:
    - qt
    - enchant

test:
  commands:
    - test -f $PREFIX/lib/libqtspell-qt5.so

about:
  home: https://github.com/manisandro/qtspell
  license: GPL-3.0-only
  license_family: GPL3
  license_file: COPYING
  summary: Spell checking for Qt text widgets

extra:
  recipe-maintainers:
    - izahn
